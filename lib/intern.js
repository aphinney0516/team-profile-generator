class intern {
    constructor(name, id, email, school){
        this.name = name;
        this.id = id;
        this.email = email;
        getName();
        getId();
        getEmail();
        getRole(); // returns "Intern"
        getSchool();
    }
}